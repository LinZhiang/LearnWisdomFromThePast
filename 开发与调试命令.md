# 开发与调试命令说明（新人版）

这个项目在根目录的 [`package.json`](./package.json) 里定义了一批快捷命令。  
你在终端里输入 **`npm run 命令名`** 就能执行，不用记一长串路径。

> **给维护者：** 以后如果改了 `package.json` 里的 `scripts`，请把本文档一起改，避免新人对着旧说明踩坑。

---

## 第一次打开项目？按这个顺序来

1. 安装**网站前端**需要的包：

   ```bash
   npm install
   ```

2. 安装**AI 后台**需要的包（只要装一次，或 `server` 目录有变动时再装）：

   ```bash
   npm run install:server
   ```

3. 如果要用 **DeepSeek / 题库里的 AI 功能**，还要配置密钥：  
   复制 `server/.env.example` 为 `server/.env`，填好里面的 Key。  
   详细说明见 [`docs/ENV-说明.md`](./docs/ENV-说明.md)。

4. 日常开发一般用这个（**网站 + AI 一起开**）：

   ```bash
   npm run dev:all
   ```

5. 浏览器打开终端里提示的地址（一般是 **`http://localhost:5173`**）。

---

## 我想……该跑哪条命令？

### 只想改页面、样式、按钮（暂时不用 AI）

```bash
npm run dev
```

浏览器访问 **`http://localhost:5173`**（端口以终端输出为准）。

---

### 要用 AI（DeepSeek 解析、测验报告等）

```bash
npm run dev:all
```

它会**同时**启动两样东西：

| 是什么 | 干什么 | 地址（默认） |
|--------|--------|----------------|
| 网站（前端） | 你在浏览器里看到的界面 | `http://localhost:5173` |
| AI 代理（后端） | 把前端的 AI 请求转给 DeepSeek，密钥只放在本机 | `http://127.0.0.1:8787` |

只开网站、不开 AI 代理时，页面上和 AI 有关的功能会连不上。

---

### 只调试 AI 后台，不 care 网页

```bash
npm run dev:api
```

改 `server/` 里的代码时用这条；网站要另开终端跑 `npm run dev`。

---

### AI 坏了，想快速查是不是 Key / 服务没起来

**先**保证 AI 代理在跑（`dev:api` 或 `dev:all`），**再**执行：

```bash
npm run check:ai
```

终端会告诉你：服务在不在、Key 配了没、最近有没有请求。  
相当于给 AI 代理做一次「体检」。

---

### 往 `src/assets/music` 里放了游戏原声（`.nwa` / `.owp`）

浏览器播不了这些格式，需要先转成普通音频：

```bash
npm run music:convert-for-web
```

- `.nwa` → 变成 `.wav`
- `.owp` → 变成 `.ogg`
- 转完会删掉原来的 nwa/owp 文件  
- **`.mid`（MIDI）不会自动转**，请自己转成 mp3/ogg/wav，或删掉

只想看看会动哪些文件、不真改磁盘：

```bash
npm run music:convert-for-web -- --dry-run
```

转完后**重新** `npm run dev` 或 `npm run build`，设置里的背景音乐列表才会更新。

---

### 改完代码，确认能正式打包上线

```bash
npm run build
```

会先检查 TypeScript 有没有写错，再把网站打包到 **`dist/`** 文件夹。  
打包成功才说明「能发布」这一侧基本没问题。

本地看一眼打包后的效果（要先 `build` 成功）：

```bash
npm run preview
```

---

### 只想查类型错误，不打包（比 build 快一点）

```bash
npm run type-check
```

---

### 提交代码前，整理代码风格

| 你想做什么 | 命令 |
|------------|------|
| 查有没有明显代码规范问题 | `npm run lint` |
| 查完并尽量自动修 | `npm run lint:fix` |
| 统一缩进、换行等格式 | `npm run format` |
| 只检查格式、不改文件 | `npm run format:check` |

---

## 所有命令一览（对照表）

| 命令 | 一句话说明 |
|------|------------|
| `npm run dev` | 只开网站 |
| `npm run dev:api` | 只开 AI 后台 |
| `npm run dev:all` | 网站 + AI 一起开（**最常用**） |
| `npm run check:ai` | 检查 AI 后台是否正常 |
| `npm run music:convert-for-web` | 把音乐资源转成浏览器能播的格式 |
| `npm run install:server` | 给 `server/` 装依赖 |
| `npm run build` | 正式打包到 `dist/` |
| `npm run preview` | 预览打包结果 |
| `npm run type-check` | 只查 TypeScript 类型 |
| `npm run lint` | 代码规范检查 |
| `npm run lint:fix` | 规范检查 + 自动修复 |
| `npm run format` | 自动排版整个项目 |
| `npm run format:check` | 只查排版、不改文件 |

---

## 两个小概念（不用背，知道即可）

**「前端 / 网站」**  
就是 Vue + Vite 这部分，你在浏览器里操作的学习网站。

**「AI 代理 / server」**  
本机跑的一个小服务，负责转发 AI 请求，避免把 API Key 写进网页代码里。  
开发时前端通过 **`/api/ai`** 访问它（Vite 会自动转到 `8787` 端口）。

---

## 遇到问题怎么办？

| 你遇到的情况 | 可以试试 |
|--------------|----------|
| AI 一直报错、没反应 | 先 `npm run dev:all`，再 `npm run check:ai` 看终端提示 |
| `npm run build` 失败 | 先 `npm run type-check`，按报错改完再 build |
| 背景音乐里看不到新加的歌 | 是不是 nwa/owp？先 `npm run music:convert-for-web`，再重启 dev |
| 命令提示找不到 `npm` | 需要先安装 [Node.js](https://nodejs.org/)（带 npm） |

---

## 在 `server` 文件夹里单独开发时

进入 `server` 目录后还可以用：

| 命令 | 说明 |
|------|------|
| `npm run start` | 启动 AI 代理（和根目录的 `dev:api` 一样） |
| `npm run dev` | 启动代理，改代码会自动重启 |

---

## 文档维护（给以后改项目的人）

- 改了 `package.json` 里的 `scripts` → 更新本文「一览表」和「我想……」相关段落  
- 改了开发端口或 AI 代理地址 → 更新「两个小概念」和 [`docs/ENV-说明.md`](./docs/ENV-说明.md)  
- 改了 `scripts/` 下脚本的行为 → 更新对应「我想……」小节  

*与当前 `package.json` 一致：共 13 条脚本（`dev` 至 `format:check`）。*
